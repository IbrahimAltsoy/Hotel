@model List<ResultSenderMessage>
@{
    ViewData["Title"] = "Inbox";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Inbox</h1>

      
                    <div class="email-left-box">
    <a href="/AdminContact/AddSenderMessage/" class="btn btn-primary btn-block">Yeni Mesj Oluştur</a>
                @await Html.PartialAsync("/Views/AdminContact/SideBarAdminContactPartial.cshtml")
                       
                @await Html.PartialAsync("/Views/AdminContact/SideBarCategoryAdminContactPartial.cshtml")
                       
                    </div>
                    <div class="email-right-box">
                        <div role="toolbar" class="toolbar">
                            <div class="btn-group">
                                <button aria-expanded="false" class="btn btn-dark" type="button">
                                    Gelen Mesajlar <span class="caret m-l-5"></span>
                                </button>
                                
                            </div>
                        </div>
                        <div class="email-list m-t-15">
                            @foreach(var item in Model){
                        <div class="message">
                <a href="/AdminContact/MessageDetailsSendBox/@item.Id">
                                <div class="col-mail col-mail-1">
                                    <div class="email-checkbox">
                                        <input type="checkbox" id="chk2">
                                        <label class="toggle" for="chk2"></label>
                                    </div><span class="star-toggle ti-star"></span>
                                </div>
                                <div class="col-mail col-mail-2">
                                    @* @if (item.Message.Length < 30)
                                    {
                                        <div class="subject">@item.Message</div>
                                    }
                                    else
                                    {
                                        <div class="subject">@item.Message.Substring(0,40) ..</div>
                                    } *@
                                    <b>Konu: @item.Title</b> Gönderen: @item.ReceiverName
                                        <div class="date">@((((DateTime)item.CreatedDate).ToString("dd-MMM-yyyy")))</div>
                                    </div>
                                </a>
                            </div>
                    }
                            
                        </div>
                        <!-- panel -->
                        
                    </div>
              
       
   
